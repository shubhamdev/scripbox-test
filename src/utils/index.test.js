import { login } from "./index";

// test("get", () => {
//   // expect(login("test")).toBe(
//   //   localStorage.setItem("TOKEN_KEY", JSON.stringify("value"))
//   // );
// });
describe("App", () => {
  it("Should be false", () => {
    const test = "Shubham";
    expect(test).toBe("Shubham");
  });
});
